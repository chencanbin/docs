---
title: "image edit"
description: ":  - Google Gemini模型接口"
openapi: "POST /v1beta/models/gemini-2.5-flash-image-preview:generateContent"
mode: "wide"
---

<Info>
**搜索关键词**: models, post, edit, gemini-2.5-flash-image-preview:generatecontent, gemini/gemini-2.5-flash-image-preview/official format, image
</Info>

# image edit

:  - Google Gemini模型接口

## 接口信息

- **请求方法**: `POST`
- **接口路径**: `/v1beta/models/gemini-2.5-flash-image-preview:generateContent`
- **认证方式**: Bearer Token
- **标签**: Gemini/gemini-2.5-flash-image-preview/Official format

## 认证说明

请在请求头中包含有效的 Bearer Token：

```bash
Authorization: Bearer YOUR_API_KEY
```

## 使用说明

使用下方的交互式 API 文档来测试此接口。你可以：

1. 在右侧面板中输入请求参数
2. 点击"Try it"按钮发送请求
3. 查看实时的响应结果

## 示例代码

### cURL 示例

```bash
curl -X POST "http://129.226.58.30/v1beta/models/gemini-2.5-flash-image-preview:generateContent" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json"
```

### Python 示例

```python
import requests

url = "http://129.226.58.30/v1beta/models/gemini-2.5-flash-image-preview:generateContent"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

response = requests.post(url, headers=headers)
print(response.json())
```

### JavaScript 示例

```javascript
const response = await fetch('http://129.226.58.30/v1beta/models/gemini-2.5-flash-image-preview:generateContent', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(data);
```

## 相关接口

搜索相关标签: `Gemini/gemini-2.5-flash-image-preview/Official format`

