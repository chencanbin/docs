---
title: "GET /api/v3/minimax/hailuo-02/pro"
description: ": API中转站服务器"
openapi: "GET /api/v3/minimax/hailuo-02/pro"
mode: "wide"
---

<Info>
**搜索关键词**: /api/v3/minimax/hailuo-02/pro, api, hailuo-02, minimax, pro, get
</Info>

# GET /api/v3/minimax/hailuo-02/pro

: API中转站服务器

## 接口信息

- **请求方法**: `GET`
- **接口路径**: `/api/v3/minimax/hailuo-02/pro`
- **认证方式**: Bearer Token
- **标签**: 无

## 认证说明

请在请求头中包含有效的 Bearer Token：

```bash
Authorization: Bearer YOUR_API_KEY
```

## 使用说明

使用下方的交互式 API 文档来测试此接口。你可以：

1. 在右侧面板中输入请求参数
2. 点击"Try it"按钮发送请求
3. 查看实时的响应结果

## 示例代码

### cURL 示例

```bash
curl -X GET "http://129.226.58.30/api/v3/minimax/hailuo-02/pro" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json"
```

### Python 示例

```python
import requests

url = "http://129.226.58.30/api/v3/minimax/hailuo-02/pro"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
print(response.json())
```

### JavaScript 示例

```javascript
const response = await fetch('http://129.226.58.30/api/v3/minimax/hailuo-02/pro', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
console.log(data);
```

## 相关接口

搜索相关标签: 无

